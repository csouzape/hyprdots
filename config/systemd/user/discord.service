[Unit]
Description=Discord
PartOf=hypr-userapps.target
After=graphical-session.target

[Service]
Environment=ELECTRON_OZONE_PLATFORM_HINT=wayland
ExecStart=/usr/bin/discord --start-minimized --ozone-platform=wayland
Restart=on-failure
RestartSec=3

[Install]
WantedBy=hypr-userapps.target
